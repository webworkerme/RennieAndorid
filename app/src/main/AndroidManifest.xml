<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.demo.festivalapp.festivalapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:hardwareAccelerated="false"
        android:icon="@drawable/splash_logo"
        android:label="Rennie"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.NotificationActvity"
            android:label="@string/title_activity_notification_actvity"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Activities.CalenderTestActivity"
            android:label="@string/title_activity_calender_test"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Launcher.SplashActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.HomeScreenActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.FestivalDetailActivty"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.EventDetailActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.FestivalEventListActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AllWishlist"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AllLocalWishlist"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.AddNewWishActivty"
            android:screenOrientation="portrait" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.MapsActivity"
            android:label="@string/title_activity_maps"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.FreeUserMapActivty"
            android:label="@string/title_activity_free_user_map_activty"
            android:screenOrientation="portrait" /> <!-- <receiver -->
        <!-- android:name="BroadcastManager" -->
        <!-- android:label="BroadcastReceiverAux"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="ALARM" /> -->
        <!-- <category android:name="android.intent.category.DEFAULT" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <activity
            android:name=".Activities.AddMarker"
            android:label="@string/title_activity_add_marker"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activities.LocalMainFestivalListActivity"
            android:screenOrientation="portrait" />

        <service
            android:name=".UtilitiesClasses.ScheduledService"
            android:enabled="true"
            android:icon="@drawable/splash_logo"
            android:label="@string/app_name" />

        <receiver android:name=".UtilitiesClasses.AlarmReceiver">
            <intent-filter>
                <action android:name="your.company.blah.mybroadcast" />
            </intent-filter>
        </receiver>
    </application>

</manifest>